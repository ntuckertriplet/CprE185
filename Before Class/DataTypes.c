#include <stdio.h>
#include <stdiolib.h>

int main(){

  //integer type, only whole numbers
  int number = 40;

  //doubles can be decimals
  double doubleNumber = 20.5;

  //still means 40, but can be divided or multiplied into a double
  double doubleNumberTwo = 40.0;

  //can only store a single character within a char type
  char letter = 'a';

  //can only hold a string in an array of chars
  char string[] = "characterString";

  
  return 0;
}
