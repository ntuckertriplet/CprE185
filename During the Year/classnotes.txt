it isn't OOP
algorithms

#include <stdio.h>
#include <stdbool.h>
#include <math.h>
#include <stdlib.h>   // Enables use of rand()
#include <string.h>
#include <ctype.h>
#include <string.h>


int main(void) {
   int seedVal;

   scanf("%d", &seedVal);

   srand(seedVal);

   printf("%d\n", rand() % 10);
   printf("%d\n", rand() % 10);

   return 0;
}



total += count

total = total + count


strings are goofy
